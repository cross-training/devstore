server:
  port: ${PORT:9001}
spring:
  application:
    name: catalog-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
  config:
    import: optional:configserver:${CONFIG_SERVICE_URI:http://localhost:8888}