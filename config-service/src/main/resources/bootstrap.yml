server:
  port: 9101
string:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cross-training/devstore.git
          searchPaths: config-data
          username: ${GIT_CROSSTRAINING_TOKEN_NAME}
          password: ${GIT_CROSSTRAINING_TOKEN_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
    # oauth2:
    #   resource:
    #     userInfoUri: http://localhost:9191/auth/user
    #   client:
    #     clientId: config-service
    #     clientSecret: config-service-secret
    #     accessTokenUri: http://localhost:9191/auth/oauth/token
    #     userAuthorizationUri: http://localhost:9191/auth/oauth/authorize
    #     grantType: client_credentials
    #     scope: server

